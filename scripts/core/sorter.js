class Sorter {
    //#region Methods
    static compareListItem(item1, item2) {
        //if ((item1 instanceof Core.listBoxItem) && (item2 instanceof Core.listBoxItem)) {
        //    if (item1.ListBox() && (typeof item1.ListBox().onCompare === Types.CONSTANTS.FUNCTION)) return item1.ListBox().onCompare(item1, item2);
        //    else return item1.text.compareTo(item2.text);
        //} else return 0;
    }
    static compareTreeItem(item1, item2) {
        //if ((item1 instanceof $j.treeViewItem) && (item2 instanceof $j.treeViewItem)) {
        //    if (item1.TreeView() && (typeof item1.TreeView().onCompare === _const.FUNCTION)) return item1.TreeView().onCompare(item1, item2);
        //    else return item1.text.compareTo(item2.text);
        //} else return 0;
    }
    //#endregion
}